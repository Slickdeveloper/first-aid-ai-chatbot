# Scripts

Utility scripts for setup, ingestion, and maintenance.

These scripts support the project workflow, especially source ingestion.

## Current Workflow

- `ingest_sources.py`: ingest canonical approved files from `data/sources/` into the database
- `setup_dev.ps1`: local development setup helper
