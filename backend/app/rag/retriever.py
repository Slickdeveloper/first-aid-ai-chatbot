"""Retriever for approved medical content.

The main retrieval implementation currently lives in `services/retrieval_service.py`.
This module remains as a placeholder if retrieval is later split into its own layer.
"""
